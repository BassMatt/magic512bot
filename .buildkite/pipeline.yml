steps:
  - label: "Example Test"
    command: echo "Hello!"
    agents:
      queue: kubernetes
    plugins:
      - kubernetes:
          gitEnvFrom:
            - secretRef:
                name: my-private-git-ssh-key